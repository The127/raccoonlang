/*
package raccoonlang.tokenizer;

import static org.junit.jupiter.api.Assertions.*;

class TokenizerTest {

    @org.junit.jupiter.api.Test
    void tokenize() {
        Tokenizer testee = new Tokenizer();
        TokenStream stream = testee.tokenize("fake", "()");
        stream.take(TokenType.OPEN_PAREN);
        stream.take(TokenType.CLOSE_PAREN);
        stream.take(TokenType.EOF);
    }
}*/
